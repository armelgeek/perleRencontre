    <div class="container-fluid">
        <div class="row">

        {% for image in lastImages %}
             <div class="col-lg-4 m-1" style="height:60px;background-image: url('{{ asset('uploads/images/' ~  image.imagepath) }}')">
             </div>
        {% endfor %}
        </div>
     </div>